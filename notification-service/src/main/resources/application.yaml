spring:
  application:
    name: notification-service
  profiles:
    active: dev

server:
  port: 8087

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}
