spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_users?useSSL=false&serverTimezone=America/Santiago
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
        dialect: org.hibernate.dialect.MySQLDialect


jwt:
  secret: tintero-social-secret-key-muy-larga-para-que-sea-segura-2026
  expiration: 86400000

logging:
  level:
    root: INFO
    cl.javiep.userservice: INFO