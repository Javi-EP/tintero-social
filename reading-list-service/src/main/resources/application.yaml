spring:
  application:
    name: reading-list-service
  profiles:
    active: dev

server:
  port: 8084

eureka:
  client:
    service-url:
      defaultZone: ${EUREKA_URI:http://localhost:8761/eureka/}