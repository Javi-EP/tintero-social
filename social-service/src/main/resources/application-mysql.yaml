spring:
  datasource:
    url: jdbc:mysql://localhost:3306/db_social?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
    driver-class-name: com.mysql.cj.jdbc.Driver
    username: root
    password:

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    database-platform: org.hibernate.dialect.MySQL8Dialect

services:
  user-service:
    url: http://localhost:8081
  review-service:
    url: http://localhost:8083
  reading-list-service:
    url: http://localhost:8084